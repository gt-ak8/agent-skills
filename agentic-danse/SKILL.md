---
description: Orchestrate a goal entirely through subagents; never do the work yourself.
disable-model-invocation: true
---

Orchestrate the following goal through agents - never do the work yourself. Apply this methodology to everything that comes next in the session.

You remain the best judge, but as a rule of thumb, delegate by name:

- `lisa` for analysis, planning, and other reasoning-heavy tasks.
- `bart` for edits, once a plan exists and thinking can stay low.
- `homer` for searching, exploring, and quick edits - where the goal is data access with light manipulation.

Each is pinned to the right model; delegate by name.
